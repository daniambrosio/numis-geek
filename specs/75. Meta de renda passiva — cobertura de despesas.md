# Spec 75 — Meta de renda passiva: cobertura de despesas

Status: 📝 Draft

Trilha Despesas: 68 → 69 → 70 → 71 → 72 → 73 → 74 → **75** (fecho da trilha). Depende da 74 (despesa média) e do lado investimentos já existente (distributions, portfolio_snapshot).

## Contexto

O objetivo declarado do user pra trilha inteira: *"ver quanto é minha despesa mensal média e traçar objetivos de investimento para cobrir essas despesas com renda passiva"*. Esta spec cruza os dois lados do sistema pela primeira vez.

Decisão da entrevista (2026-08-09): **yield alvo fixo configurável**, com cenários:

```
patrimônio_meta = despesa_anual / yield_alvo
```

Ex.: R$ 15k/mês × 12 ÷ 6% = R$ 3M. Cenários default: conservador 4%, base 6%, otimista 8% — editáveis.

## Modelo

`workspace_setting` (ou tabela dedicada `passive_income_config`, 1 row por workspace):

| coluna | notas |
|---|---|
| workspace_id | PK/FK |
| yield_scenarios | JSON `[{"label":"Conservador","rate":0.04},{"label":"Base","rate":0.06},{"label":"Otimista","rate":0.08}]` |
| updated_at/by | |

## Cálculos (serviço `passive_income_summary`)

Insumos:

- `despesa_mensal_média` — `avg_monthly_expense_12m` da spec 74.
- `renda_passiva_12m` — soma de `distribution.net` dos últimos 12 meses fechados (dado que já existe do lado investimentos), ÷ 12 = renda passiva mensal média.
- `patrimônio_atual` — `total_value_brl/usd` do último `portfolio_snapshot` CLOSED.

Saídas por cenário:

| métrica | fórmula |
|---|---|
| Patrimônio meta | `despesa_anual / rate` |
| Progresso | `patrimônio_atual / patrimônio_meta` |
| Gap | `patrimônio_meta − patrimônio_atual` |

E independente de cenário:

| métrica | fórmula |
|---|---|
| **Cobertura atual** | `renda_passiva_mensal / despesa_mensal_média` — o número-farol ("hoje a renda passiva cobre X% das despesas") |
| Yield real 12m | `renda_passiva_12m / patrimônio_médio_12m` (média dos snapshots CLOSED do período) — mostrado como referência ao lado dos cenários teóricos |

Tudo dual-currency (despesas em `amount_brl`, distributions/snapshot já têm brl/usd).

## API + Frontend

- `GET /api/passive-income/summary` + `PUT /api/passive-income/config` (admin+sysadmin).
- Página `/independencia` (sidebar: **Independência**):
  1. **Hero — Cobertura**: "Sua renda passiva cobre **X%** da sua despesa média" com barra de progresso; renda passiva mensal vs despesa mensal lado a lado.
  2. **Cards de cenário** (um por yield): patrimônio meta, progresso %, gap; badge com o yield real 12m pra comparação.
  3. Editor de cenários (modal, ESC, spinner, toast).
- Card resumido no Dashboard principal (mesmo padrão dos cards existentes) linkando pra `/independencia`.

## Fora de escopo

- Projeção temporal "em que ano chego lá" com aportes + juros compostos — spec futura se o user quiser (opção "meta em anos" foi discutida e adiada na entrevista).
- Metas por país/classe (feature 17 do CLAUDE.md).

## Deliverables

1. Config + serviço + endpoint + testes pytest (cenários, cobertura, yield real, janela 12m, workspace sem snapshot/sem despesa → estados vazios sãos).
2. Página `/independencia` + card no dashboard + testes de componente.
