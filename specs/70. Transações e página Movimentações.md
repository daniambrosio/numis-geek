# Spec 70 — Transações e página Movimentações

## Status: 📝 Draft

Trilha Despesas (ordem de implementação): 68 → 69 → **70** → 72 → 73 → 71 → 74 → 75. Depende de 68 (category/party/tag) e 69 (credit_card_account/invoice).

## Contexto

A entidade central do lado Numis. Decisões da entrevista (2026-08-09):

- **Tabela única** `transaction` com discriminador `source_type` (checking | credit_card) — ledger unificado sai natural, como a página Movimentações do protótipo.
- **Pagamento de fatura** = cash tx com FK explícito `paid_invoice_id`; contrato único via serviço `pay_invoice` (abaixo); não conta como despesa (é transferência).
- **Parcelamento** = N transações (uma por parcela, cada uma na sua fatura/mês) + `parent_transaction_id` apontando pra 1ª parcela.
- **Multi-moeda** = cotação real do extrato quando disponível, PTAX fallback; grava `fx_rate` + valores em ambas as moedas (invariante do projeto: todo lançamento tem fx_rate; tudo exibível em BRL e USD).

## Modelo

### `transaction`

| coluna | tipo | notas |
|---|---|---|
| id | String(36) uuid PK | |
| workspace_id | FK workspace | |
| source_type | Enum `CHECKING \| CREDIT_CARD` | |
| checking_account_id | FK account nullable | exatamente um dos dois FKs preenchido (CHECK constraint + validação de serviço); deve ser account_type=checking |
| credit_card_account_id | FK credit_card_account nullable | convenção `<tabela>_id` |
| invoice_id | FK invoice nullable | obrigatório quando CREDIT_CARD |
| event_date | Date | data da compra/movimento (parcelas futuras têm event_date futuro — ver nota abaixo) |
| description | String(255) | |
| amount | Numeric(18,2) | **signed**: despesa negativa, receita positiva (convenção do protótipo) |
| currency | Enum Currency | moeda nativa da tx |
| fx_rate | Numeric(18,8) | padrão do repo (distribution/asset_movement); PTAX walkback via helper existente `resolve_fx_rate` (`services/fx.py`) |
| fx_source | Enum `STATEMENT \| PTAX` | STATEMENT quando a cotação veio do documento (ex.: fatura "US$ 89 @ R$ 5,15") |
| amount_brl / amount_usd | Numeric(18,2) | derivadas **persistidas** na escrita — deliberado (diverge do padrão derive-em-query do lado investimentos): a cotação STATEMENT vem do documento e não é recomputável a partir da tabela PTAX |
| category_id | FK category nullable | |
| party_id | FK party nullable | |
| installment_number / installment_total | Integer nullable | "5/12" |
| parent_transaction_id | FK transaction nullable | 1ª parcela tem parent NULL; parcelas 2..N apontam pra 1ª |
| paid_invoice_id | FK invoice nullable | só em cash tx que paga fatura |
| transfer_group_id | String(36) nullable | mesmo id nas duas pernas de uma transferência entre contas próprias; usado como exclusão estrutural na média (spec 74) |
| fingerprint | String(40) indexed nullable-false | sha1(`source_type` + FK da origem + `event_date` + `amount` + `currency` + `normalize_description(description)`) — **computado na escrita de TODA tx, manual ou importada** (helper da spec 68). Não-único (duplicatas legítimas recebem sufixo `:2` no import, spec 71) |
| is_reconciled | Boolean default false | true quando veio de/foi casada com arquivo importado (spec 71) |
| notes | Text nullable | ex. "US$ 89,00 @ R$ 5,15 + IOF R$ 29,40" |
| timestamps/by | | padrão |

Índices: `(workspace_id, event_date)`, `(invoice_id)`, `(category_id)`, `(party_id)`, `(workspace_id, fingerprint)`.

`transaction_tag(transaction_id, tag_id)` — join table da spec 68, criada aqui.

Migrations adicionais: `invoice.paid_by_transaction_id` (FK transaction, nullable) via `op.batch_alter_table`; `AttachmentSourceType` ganha valor `TRANSACTION` (batch_alter no enum) — composer permite anexar comprovante à tx.

> **Nota — event_date futuro**: parcelas de cartão ainda não faturadas existem como tx com `event_date` futuro (compromisso já contratado). Todo summary/KPI "até hoje" filtra `event_date <= today`; a média da spec 74 usa apenas meses fechados, então parcelas futuras nunca a poluem. Isso é uma exceção deliberada à premissa "Transaction = passada" (spec 72) — vale só pra parcelas de cartão.

### Contrato único de pagamento de fatura (resolve o conflito 70×71)

Serviço **`pay_invoice(invoice, checking_account, date, amount)`** — o ÚNICO caminho que quita fatura, usado tanto pelo botão da UI quanto pela confirmação do import (spec 71):

1. cria a cash tx (categoria Transferência, `paid_invoice_id` preenchido, fingerprint computado);
2. seta `invoice.status=PAID`, `paid_at`, `paid_by_transaction_id`;
3. atômico; auditado.

**Lado autoritativo**: `transaction.paid_invoice_id`. `invoice.paid_by_transaction_id` é espelho denormalizado mantido exclusivamente por este serviço. Como o fingerprint existe em toda tx, um pagamento manual seguido do import do extrato cai no dedup camada 2 da spec 71 — sem duplicação.

### Demais regras de serviço

- Criar card tx sem invoice → resolve/cria a invoice OPEN do período (regra da spec 69).
- **Parcelamento no composer**: input "R$ 12.000 em 12x a partir da fatura X" → cria 12 tx de 1.000, `installment_number` 1..12, cada uma na invoice do mês correspondente (cria OPEN futuras conforme necessário), filhas apontando pra 1ª.
- Toda escrita audita (audit_log padrão).

## API

- `GET /api/transactions` — filtros: date range, source (contas+cartões), category, party, tag, search, reconciled. Paginado, ordenado desc.
- `POST/PATCH/DELETE /api/transactions` — delete de pai de parcelamento pergunta em cascata (query param `cascade=true`).
- `GET /api/transactions/summary?from&to` — entradas, saídas, resultado, não-reconciliadas (KPIs da página), em BRL e USD, com `event_date <= today`. **Nota de reconciliação**: este summary é o *fluxo bruto* (inclui transferências e pagamentos de fatura); o `/api/expenses/summary` da spec 74 aplica exclusões — os dois números divergem por definição.

## Frontend — protótipo como base, deltas declarados abaixo

- **`/transactions`** (Movimentações — rota EN canônica já reservada) substitui o ComingSoon: 4 KPIs com os labels do proto (Entradas · período, Saídas · período, Resultado, Não reconciliadas — l.5442-5459), botão "Reconciliar" no header (l.5434), card de filtros (busca, date range, chips de Origem = contas checking + cartões, chips de Categoria), tabela agrupada por mês (MonthHeader) com colunas Data / Origem (logo FI + ícone cartão + parc. X/Y) / Descrição (+note) / Categoria (pill colorida, "Sem categoria" itálico) / Valor signed / Status (check verde ou dot âmbar) / coluna de ações (⋯).
- Composer: habilitar "Movimentação" (cash) e "Lançamento de cartão"; ambos com `NotesAttachmentsField` e bloco de preview como no proto (l.6101-6218). ESC fecha; submit com disabled+spinner; erro em toast.
- **`/accounts/:id`** — página de detalhe da conta checking (**esta spec é a dona da rota**; specs 71 e 76 linkam pra cá): conforme `ContaDetail` do proto (l.3311-3464) — hero com saldo + sparkline, KPIs, lista de movimentações da conta. O card "Orçamento" do proto fica de fora (spec futura de budget) — delta declarado.
- `/credit-cards/:id`: tabela "Lançamentos da fatura aberta" passa a ser real; botão "Pagar fatura" nas faturas CLOSED.

### Deltas declarados vs protótipo

- **Gerador de parcelas** ("12x a partir da fatura X"): o proto tem só campo texto livre "Parcela · ex: 1/3" (l.6294-6296). O gerador é design novo, deliberado — digitar 12 tx na mão não escala.
- **Botão "Pagar fatura"**: não existe no proto (que nem modela pagamento); necessário pelo contrato `pay_invoice`.
- **Filtro por Party** nas Movimentações: proto só tem Origem/Categoria (l.5469-5474); adicionamos Party (dropdown) — extensão deliberada. **Tags não ganham filtro de UI no MVP** (só via API).
- **Auto-categorização** (`suggestedCat` do proto): fora do MVP — categoria é manual no composer (delta declarado, mesmo racional da spec 68).

## Fora de escopo

- Import de arquivos (spec 71). Auto-categorização. Orçamento.

## Deliverables

1. Migrations + model `transaction` (com `fingerprint`, `transfer_group_id`) + `transaction_tag` + `invoice.paid_by_transaction_id` + `AttachmentSourceType.TRANSACTION`.
2. Serviços (`pay_invoice`, parcelamento, resolve-invoice) + API + testes pytest (CHECK do FK exclusivo, parcelas nas invoices certas, pay atômico e único caminho, fingerprint em tx manual, fx_rate sempre resolvido, summary com event_date <= today).
3. Páginas Movimentações + `/accounts/:id` + composers + testes de componente (incluindo shape real do backend — lição jsdom: campos undefined derrubam modal).
