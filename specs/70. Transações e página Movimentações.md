# Spec 70 — Transações e página Movimentações

Status: 📝 Draft

Trilha Despesas: 68 → 69 → **70** → 71 → 72 → 73 → 74 → 75. Depende de 68 (category/party/tag) e 69 (credit_card_account/invoice).

## Contexto

A entidade central do lado Numis. Decisões da entrevista (2026-08-09):

- **Tabela única** `transaction` com discriminador `source_type` (checking | credit_card) — ledger unificado sai natural, como a página Movimentações do protótipo.
- **Pagamento de fatura** = cash tx com FK explícito `paid_invoice_id` → invoice vira PAID; não conta como despesa (é transferência).
- **Parcelamento** = N transações (uma por parcela, cada uma na sua fatura/mês) + `parent_transaction_id` apontando pra 1ª parcela.
- **Multi-moeda** = cotação real do extrato quando disponível, PTAX fallback; grava `fx_rate` + valores em ambas as moedas (invariante do projeto: todo lançamento tem fx_rate; tudo exibível em BRL e USD).

## Modelo

### `transaction`

| coluna | tipo | notas |
|---|---|---|
| id | String(36) uuid PK | |
| workspace_id | FK workspace | |
| source_type | Enum `CHECKING \| CREDIT_CARD` | |
| checking_account_id | FK account nullable | exatamente um dos dois FKs preenchido (CHECK constraint + validação de serviço); deve ser account_type=checking |
| credit_card_id | FK credit_card_account nullable | |
| invoice_id | FK invoice nullable | obrigatório quando CREDIT_CARD |
| event_date | Date | data da compra/movimento |
| description | String(255) | |
| amount | Numeric(18,2) | **signed**: despesa negativa, receita positiva (convenção do protótipo) |
| currency | Enum Currency | moeda nativa da tx |
| fx_rate | Numeric(12,6) | BRL por USD na data; 1.0-equivalente resolvido sempre (PTAX walkback via helper existente `resolve_fx_rate`) |
| fx_source | Enum `STATEMENT \| PTAX` | STATEMENT quando a cotação veio do documento (ex.: fatura "US$ 89 @ R$ 5,15") |
| amount_brl / amount_usd | Numeric(18,2) | derivadas persistidas na escrita |
| category_id | FK category nullable | |
| party_id | FK party nullable | |
| installment_number / installment_total | Integer nullable | "5/12" |
| parent_transaction_id | FK transaction nullable | 1ª parcela é o pai de si mesma? Não — pai é a 1ª parcela; ela tem parent NULL |
| paid_invoice_id | FK invoice nullable | só em cash tx que paga fatura |
| is_reconciled | Boolean default false | true quando veio de/foi casada com arquivo importado (spec 71) |
| notes | Text nullable | ex. "US$ 89,00 @ R$ 5,15 + IOF R$ 29,40" |
| timestamps/by | | padrão |

Índices: `(workspace_id, event_date)`, `(invoice_id)`, `(category_id)`, `(party_id)`.

`transaction_tag(transaction_id, tag_id)` — join table da spec 68, criada aqui.

Migration adicional: `invoice.paid_by_transaction_id` (FK transaction, nullable) via `op.batch_alter_table`.

### Regras de serviço

- Criar card tx sem invoice → resolve/cria a invoice OPEN do período (regra da spec 69).
- **Parcelamento no composer**: input "R$ 12.000 em 12x a partir da fatura X" → cria 12 tx de 1.000, `installment_number` 1..12, cada uma na invoice do mês correspondente (cria OPEN futuras conforme necessário), filhas apontando pra 1ª.
- **Pagar fatura**: `POST /api/invoices/{id}/pay` body `{ checking_account_id, date, amount }` → cria cash tx (categoria Transferência, `paid_invoice_id`), seta invoice PAID/paid_at/paid_by_transaction_id. Atômico.
- Toda escrita audita (audit_log padrão).

## API

- `GET /api/transactions` — filtros: date range, source (contas+cartões), category, party, tag, search, reconciled. Paginado, ordenado desc.
- `POST/PATCH/DELETE /api/transactions` — delete de pai de parcelamento pergunta em cascata (query param `cascade=true`).
- `GET /api/transactions/summary?from&to` — entradas, saídas, resultado, não-reconciliadas (KPIs da página), em BRL e USD.

## Frontend — seguir protótipo fielmente

- `/transactions` (Movimentações) substitui o ComingSoon: 4 KPIs (Entradas, Saídas, Resultado, Não reconciliadas), card de filtros (busca, date range, chips de Origem = contas checking + cartões, chips de Categoria), tabela agrupada por mês (MonthHeader) com colunas Data / Origem (logo FI + ícone cartão + parc. X/Y) / Descrição (+note) / Categoria (pill colorida, "Sem categoria" itálico) / Valor signed / Status (check verde ou dot âmbar).
- Composer: habilitar "Movimentação" (cash) e "Lançamento de cartão" (com campo parcelas). ESC fecha; submit com disabled+spinner; erro em toast.
- `/cartao/:id`: tabela "Lançamentos da fatura aberta" passa a ser real; botão "Pagar fatura" nas faturas CLOSED.
- Página de conta checking (`/conta/:id`): lista de movimentações da conta.

## Fora de escopo

- Import de arquivos (spec 71). Auto-categorização. Orçamento.

## Deliverables

1. Migration + model `transaction` + `transaction_tag` + `invoice.paid_by_transaction_id`.
2. Serviços (parcelamento, pay-invoice, resolve-invoice) + API + testes pytest (CHECK do FK exclusivo, parcelas nas invoices certas, pay atômico, fx_rate sempre resolvido, summary exclui nada ainda — exclusões de média são spec 74).
3. Página Movimentações + composers + testes de componente (incluindo shape real do backend — lição jsdom: campos undefined derrubam modal).
