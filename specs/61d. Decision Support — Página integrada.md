# Spec 61d — Decision Support: Página integrada + docs

## Status: 📝 Draft — depende de 61b + 61c

## Priority

Conclui o feature loop — substitui placeholder, atualiza doc rationale.

## Why

Após 61a (targets), 61b (valuation engine) e 61c (Markowitz solver), os
três componentes precisam ser apresentados juntos na rota
`/decision-support` (hoje placeholder). Esta spec é a "glue" — wiring
final + atualização documental.

## Scope

### Frontend

- Substituir `pages/DecisionSupport.tsx` (hoje placeholder ComingSoon)
  pelo layout 3-cards do plano master:
  - Card 1: `<GapVsTargetChart>` + `<GapTable>` (componentes de 61c)
  - Card 2: Markowitz — `<OptimizationControls>` +
    `<EfficientFrontierChart>` + `<SuggestedTradesTable>`
  - Card 3: `<ValuationTable>` (componente de 61b) com lista de assets
    owned + verdict + métricas expansíveis.
- Remover `placeholder: true` do nav item em `AppLayout.tsx`.
- Empty states:
  - Sem snapshots suficientes (<12) → mostrar onboarding "Faça pelo
    menos 12 fechamentos pra Markowitz funcionar".
  - Sem targets cadastrados → CTA "Configure suas metas em /admin/
    target-allocation".
  - Sem fundamentals em nenhum ativo → seção (c) mostra "Aguardando
    primeiro refresh diário".

### Docs

- `docs/decision-support-rationale.md`:
  - **§9 (Out of scope)**: remover bullet "Otimização de portfólio
    markowitzeana — complexidade vs benefício ruim pro caso de uso".
  - **§10 #4**: ajustar pra "APIs integradas desde v1 (brapi/Finnhub/
    yfinance fundamentals + history); FMP/Fundamentus/CoinGecko ficam
    v2".
  - **§10 #1**: notar que TargetAllocation foi implementada com UI de
    edição manual em `/admin/target-allocation`; seed do Notion adiado
    indefinidamente.
  - **§10 #5**: ajustar pra "Watchlist v1.5+".
  - **Acrescentar §15 Markowitz v1 — formulação**: min-variance,
    equality por classe, cap 15%/ativo, cap 70% país, no-short,
    Ledoit-Wolf α=0.05, scipy SLSQP, BRL como unit, mensal v1.
  - **Acrescentar §16 Valuation v1**: ativos owned, calculadoras Bazin/
    Graham/Lynch/P-VP/P-FFO/DY, verdict 3 níveis (rationale §12),
    fundamentals via brapi+Finnhub+yfinance, cron diário 19h SP.

### Tests

- `pages/DecisionSupport.test.tsx` (substituir teste atual se houver):
  render happy path com fetch stubbed, empty states (no snapshots, no
  targets, no fundamentals), error state.

## Out of Scope (defer)

- Snowflake/radar (v1.5+).
- Cash disponível + reserva de emergência (v1.5+).
- Top opportunities ranking (v1.5+).
- Watchlist integration (v1.5+).
- "Plano de compras" / shopping cart mental (v2).

## References

- Spec master 61.
- Specs 61a, 61b, 61c — dependências.
- `docs/decision-support-rationale.md` — doc a ser atualizado.
- Memory `follow_prototype_strictly` — se existir protótipo HTML,
  reproduzir fielmente (não existe atualmente pra Decision Support).
