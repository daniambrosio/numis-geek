# Spec 61b — Decision Support: Valuation Engine

## Status: ✅ Done (2026-06-29) — implementação overnight com decisões assumidas (sem entrevista própria)

## Priority

Alta — entrega seção (c) "Valuation por ativo" da página Decision Support
(rumo a verdict Comprar/Manter/Vender por classe) e habilita reuso no
AssetDetail.

## Why

Hoje não há nenhum cálculo de valuation no Numis-Geek — só preço atual e
distribuições. Decision Support v1 precisa de verdict por ativo, baseado
em fundamentals (P/E, P/VP, ROE, DPA, payout, FFO, etc.) que vêm de
provedores externos.

## Scope (proposto, revisa na entrevista)

### Schema

- Tabela nova `asset_fundamentals` — snapshot temporal de fundamentos
  por ativo + source. Schema completo em
  `docs/decision-support-rationale.md` §6 (campos por classe: stocks,
  REITs, ETFs, fixed income). UNIQUE(asset_id, snapshot_date, source).

### Integrations a estender/criar

- `integrations/brapi.py` — adicionar `fetch_fundamentals(ticker)` usando
  `/api/quote/:ticker?fundamental=true&modules=...`. Verificar quais
  módulos estão disponíveis no tier do user.
- `integrations/finnhub.py` — adicionar `fetch_basic_financials(symbol)`
  usando `/api/v1/stock/metric?symbol=X&metric=all`.
- `integrations/yfinance.py` — novo cliente. Métodos: `fetch_fundamentals`,
  `fetch_history_monthly`. Sem credencial (scrape público).

### Service

- `services/valuation.py` — calculadoras pure-fn (Bazin, Graham, Lynch
  PEG) + orquestradores por classe (`value_stock`, `value_reit`,
  `value_etf`, `value_fixed_income`, `value_fund`, `value_crypto`) +
  entry-point `value_asset`. Required yields default: BRL=8%, USD=5%
  (módulo `valuation_settings.py`).
- `services/fundamentals_ingest.py` — orquestra brapi/Finnhub/yfinance
  por classe+país, popula `asset_fundamentals`. Cron 19:00 SP.

### API endpoints

- `GET /api/assets/{id}/valuation` → ValuationResult (verdict + metrics).
- `GET /api/assets/{id}/fundamentals?as_of=YYYY-MM-DD` → snapshot mais
  recente (ou em data específica).
- `POST /api/assets/{id}/fundamentals:refresh` — refresh on-demand
  (admin/sysadmin).

### Frontend

- `components/ValuationCard.tsx` (ou `ValuationTable.tsx`) reusado em:
  - `pages/AssetDetail.tsx` — novo card "Valuation".
  - Seção (c) da `pages/DecisionSupport.tsx` (Spec 61d).
- `components/VerdictBadge.tsx` — pill verde/cinza/vermelho com
  tooltip de `verdict_reason`.

### Cron

- `scheduler.py` — novo job `fundamentals_refresh_daily` 19:00 SP
  (entre price refresh 18:00 e PTAX 20:00).

## Open Questions (entrevistar)

- **brapi tier:** confirmar quais módulos fundamentals estão disponíveis
  no token atual. Se incompleto, considerar Fundamentus scraper como
  fallback (free, throttle manual).
- **Finnhub fundamentals coverage:** P/FFO de REITs US não vem direto —
  precisa derivar de `ffoPerShare × price`. Aceitar derive ou marcar
  None?
- **yfinance scraping legal/operacional:** ToS, rate limits, robustez.
  Alternativa: brapi global (paid).
- **Verdict rules calibration:** rationale §12 tem rascunho. Revisar com
  user antes de virar código. Especialmente:
  - Disqualifying gates (ROE<0, Dívida/EBITDA>5x) — thresholds.
  - Required yield default (8% BRL / 5% USD) — confirmar.
- **Stale fundamentals threshold:** quando exibir badge amarelo
  "fundamentos antigos"? 90d sugerido.
- **Fundamentals manuais:** UI pra editar manualmente quando provider
  falhar? Ou só refresh on-demand?
- **Asset.sector:** popular automaticamente do provider? Adicionar
  coluna? (Fica fora se complicar.)

## Out of Scope (defer)

- Calibração via backtest (compara recomendação histórica vs realizado).
- Fundamentos pra crypto (sem provider gratuito sólido).
- Real estate / vehicle — não suportam valuation fundamentalista.
- DCF 2-stage (complexidade alta).
- Sectoral peer comparison.

## Decisions taken autonomously (overnight 2026-06-29)

User explicitly delegated escolhas (`assuma o seu caminho recomendado`):

- **Fundamentus scraper rejeitado.** Legal grey area + scraping frágil.
  brapi `?fundamental=true` cobre o suficiente pra v1 BR; quando faltar,
  user cadastra MANUAL via futuro UI (não nesta spec).
- **yfinance integrado.** Instalado como dep core (não optional). Usado
  só pra US ETF (expense_ratio + AUM) que Finnhub não cobre limpo.
- **Required yields fixos no módulo `valuation_settings`.** BRL=8%,
  USD=5%, sem overrides ainda. Spec 61.5 introduz workspace settings.
- **Verdict rules §12 do rationale aplicadas ipsis litteris.** Sem
  calibração ainda — gates (ROE<0, dívida/EBITDA>5, lucros<0 5y) ativos.
- **ETF e FIXED_INCOME retornam verdict=NA v1.** Mostram métricas
  informacionais. Sem rule engine pra ETF agora (decisão segue alocação).
- **brapi response.raw_payload truncado a 8KB.** Defensivo contra
  payloads grandes; suficiente pra debug.
- **Cron único `fundamentals_refresh_daily` 19:00 SP.** Sem catchup
  startup (não é hard-dep de outras features). max_instances=1.
- **Refresh on-demand admin/sysadmin only.** Member não pode forçar
  hit no provider (rate limit shared).
- **Sem UI manual de edição de fundamentals v1.** Refresh button apenas.
  Quando user precisar override manual, spec 61.5.

## References

- Spec master 61 — escopo Decision Support.
- `docs/decision-support-rationale.md` §3 (métodos por classe), §6
  (schema fundamentals), §12 (regras de verdict).
- Spec 61a — Target Allocation (não bloqueia, mas é peer).
- Spec 61c — Markowitz (independente).
- Memory `external_apis_strategy` — providers.
