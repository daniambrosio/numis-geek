# Spec 69 — Cartões de Crédito e Faturas

Status: 📝 Draft

Trilha Despesas: 68 → **69** → 70 → 71 → 72 → 73 → 74 → 75. Depende da 68 (nada de modelo, só a sidebar/grupo de navegação nascer junto).

## Contexto

Decisão da entrevista (2026-08-09): cartão de crédito é **entidade separada** (`credit_card_account`), como o protótipo v2 fez (`CARDS` separado de `ACCOUNTS`) — e não `Account.account_type='credit_card'` como o INITIALSPEC v1 sugeria. Campos próprios (limite, dia de fechamento/vencimento, bandeira) não contaminam a tabela `account`.

Fatura (`invoice`) é o agrupador mensal das transações de cartão. As transações em si chegam na spec 70; o import de fatura PDF na 71.

## Modelo

### `credit_card_account`

| coluna | tipo | notas |
|---|---|---|
| id | String(36) uuid PK | |
| workspace_id | FK workspace | |
| financial_institution_id | FK financial_institution | FK direto pra FI — sem Account no meio |
| name | String(100) | "Itaú Visa Infinite" |
| brand | String(30) nullable | Visa, Mastercard, Amex… |
| last4 | String(4) nullable | |
| currency | Enum Currency (reusa de `account.py`) | |
| credit_limit | Numeric(18,2) nullable | |
| close_day | Integer 1–28 | dia de fechamento |
| due_day | Integer 1–28 | dia de vencimento |
| is_active | Boolean | |
| timestamps/by | | padrão |

### `invoice`

| coluna | tipo | notas |
|---|---|---|
| id | String(36) uuid PK | |
| workspace_id | FK workspace | |
| credit_card_id | FK credit_card_account | |
| close_date | Date | data real de fechamento (pode divergir do close_day teórico — fim de semana/feriado) |
| due_date | Date | |
| total_amount | Numeric(18,2) nullable | preenchido no fechamento ou vindo do PDF (spec 71); enquanto OPEN é derivado da soma das tx |
| currency | Enum Currency | |
| status | Enum `OPEN \| CLOSED \| PAID` | |
| paid_at | Date nullable | |
| notes | Text nullable | |
| timestamps/by | | |

Constraint: única `(credit_card_id, close_date)`.

`paid_by_transaction_id` (link pra transação de pagamento na conta corrente) entra na **spec 70** via `batch_alter_table` — a tabela `transaction` ainda não existe aqui.

### Regras de ciclo

- **Invoice OPEN corrente**: criada lazy — na primeira tx de cartão do período (spec 70) ou no import (spec 71). `close_date` teórico = próximo `close_day`; ajustável.
- Período da fatura = `(close_date anterior, close_date]`.
- Fechamento: ação manual "Fechar fatura" (ou implícito no import do PDF da fatura fechada) → `status=CLOSED`, congela `total_amount`.
- PAID acontece na spec 70 (pagamento via cash tx).

## Backend

- CRUD `/api/credit-cards` (padrão do repo, workspace-scoped, admin+sysadmin escrevem).
- `/api/credit-cards/{id}/invoices` — list; `POST .../invoices` cria manual; `POST /api/invoices/{id}/close`.
- Derived no serializer do cartão: `open_invoice_total` (soma tx da OPEN — 0 até a spec 70), `limit_used_pct`.

## Frontend — seguir protótipo fielmente (REGRA DURA)

- `/cartoes` — lista de cartões: logo FI, nome, ····last4, fatura aberta, barra de limite utilizado, close/due day. KPIs topo: total em fatura aberta, % limite total.
- `/cartao/:id` — detalhe: hero com fatura aberta + vence dia X, barra de limite, KPIs (lançamentos, IOF, parceladas, próx. fatura estimada — os dois últimos podem ficar `—` até specs 70/71); card lateral "Faturas anteriores" com status paga/pendente; navegação entre cartões.
- `/faturas` — lista geral de faturas por status/mês.
- Sidebar: itens Cartões e Faturas com badge de contagem (protótipo linhas ~1701-1703). Composer ganha entry "Cartão" (Cadastros) habilitado; "Lançamento de cartão" continua disabled até a 70.
- Botões "Importar fatura" renderizam mas ficam disabled com tooltip "spec 71" até o import existir.

## Deliverables

1. Migration + models `credit_card_account`, `invoice`.
2. CRUD API + close endpoint + testes (unicidade close_date, scoping, close congela total).
3. Páginas `/cartoes`, `/cartao/:id`, `/faturas` + composer "Cartão" + testes de componente.
